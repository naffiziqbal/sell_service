import React from 'react';

const ServiceItemOnServicePage = ({service}) => {
    return (
        <div>
            <p>Title: {service.title}</p>
        </div>
    );
};

export default ServiceItemOnServicePage;
